# Alchemy with Vocs

This documentation site is built by Vocs and deployed to Railway
with the first-class `Railway.Website.Vocs` resource.

No Railway configuration or Vocs adapter setup is required.
