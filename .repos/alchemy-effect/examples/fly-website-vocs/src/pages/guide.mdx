# Deployment guide

Run `bun run deploy` to build the Vocs project and deploy its server runtime,
prerendered documentation pages, generated files, and public assets.

Unchanged source trees skip the build and deployment on subsequent runs.
