/// <reference types="astro/client" />

/** Replaced by the user `vite.define` setting in astro.config.mjs. */
declare const __USER_VITE_DEFINE__: string;

declare namespace App {
  interface Locals {
    fromMiddleware?: string;
  }
}
