import { Counter } from "../components/Counter";

# Counter

<div data-testid="counter-page-marker">COUNTER_PAGE_MARKER</div>

A custom `"use client"` React component embedded in MDX, hydrated in the
browser:

<Counter />
