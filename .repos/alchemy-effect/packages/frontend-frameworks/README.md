# @alchemy.run/frontend-frameworks

Framework build and development integrations for Alchemy.

Use `@alchemy.run/frontend-frameworks/core` for the shared framework contracts, or a
framework subpath: `/astro`, `/nextjs`, `/nuxt`, `/octane`, `/sveltekit`, or
`/waku`.
